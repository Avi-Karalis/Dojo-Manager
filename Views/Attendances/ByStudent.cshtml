@model DojoManager.ViewModels.AttendanceIndexViewModel
@{ ViewData["Title"] = "Student Attendance for " + (Model.Attendances.FirstOrDefault()?.Student?.Name ?? "Unknown"); }

<h2>Attendance for @Model.Attendances.FirstOrDefault()?.Student?.Name</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                <a href="@Url.Action("ByStudent", new { id = Model.StudentId, page = 1, pageSize = Model.PageSize, sortBy = "date", sortDir = Model.NextDir("date") })" class="btn btn-sm btn-outline-secondary">
                    Date@(Model.SortIndicator("date"))
                </a>
            </th>
            <th>
                <a href="@Url.Action("ByStudent", new { id = Model.StudentId, page = 1, pageSize = Model.PageSize, sortBy = "location", sortDir = Model.NextDir("location") })" class="btn btn-sm btn-outline-secondary">
                    Location@(Model.SortIndicator("location"))
                </a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (Attendance a in Model.Attendances)
        {
            <tr>
                <td>@a.Session!.Date.ToString("yyyy-MM-dd, dddd")</td>
                <td>@a.Session.Location</td>
            </tr>
        }
    </tbody>
</table>

<div class="d-flex align-items-center gap-2 flex-wrap mt-2">
    @if (Model.HasPrev)
    {
        <a href="@Url.Action("ByStudent", new { id = Model.StudentId, page = Model.Page - 1, pageSize = Model.PageSize, sortBy = Model.SortBy, sortDir = Model.SortDir })" class="btn btn-outline-secondary btn-sm">← Prev</a>
    }
    <span class="mx-1">Page @Model.Page of @Model.TotalPages (@Model.TotalCount total)</span>
    @if (Model.HasNext)
    {
        <a href="@Url.Action("ByStudent", new { id = Model.StudentId, page = Model.Page + 1, pageSize = Model.PageSize, sortBy = Model.SortBy, sortDir = Model.SortDir })" class="btn btn-outline-secondary btn-sm">Next →</a>
    }
    <div class="ms-3 d-flex align-items-center gap-1">
        <span class="me-1">Per page:</span>
        @foreach (var size in new[] { 5, 10, 25, 50 })
        {
            <a href="@Url.Action("ByStudent", new { id = Model.StudentId, page = 1, pageSize = size, sortBy = Model.SortBy, sortDir = Model.SortDir })"
               class="btn btn-sm @(Model.PageSize == size ? "btn-secondary" : "btn-outline-secondary")">@size</a>
        }
    </div>
</div>